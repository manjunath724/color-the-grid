<%= form_with(model: Color.new, url: colors_path) do |form| %>
  <%= form.label :hex, 'Color: ' %>
  <%= form.color_field :hex %>

  <%= form.submit 'Add Color', class: 'btn btn-sm btn-success' %>
<% end %>
